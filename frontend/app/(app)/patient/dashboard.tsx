// Role-specific dashboard routes — all delegate to the unified dashboard.
// Iteration 2 will split each into role-tailored experiences.
import Dashboard from "../dashboard";
export default Dashboard;
